# first install separately: torch torchvision 

# core DL stack
accelerate>=0.34
transformers>=4.40
timm>=1.0
huggingface-hub>=0.24
loralib>=0.1.2

# medical imaging
monai>=1.3
nibabel>=5.0

# data + metrics
numpy>=1.26
pandas>=2.0
openpyxl  # pandas xlsx support
scipy>=1.11
scikit-learn>=1.3

# config / serialization
pyyaml>=6.0
omegaconf>=2.3
attrs>=23.0
msgpack>=1.0
lmdb>=1.4
pydantic>=2.0
orjson>=3.10
pybase64>=1.4

# logging / progress / CLI
loguru>=0.7
tqdm>=4.66
natsort>=8.4

# tracking
wandb>=0.16

# rate: LLM report-structuring pipeline
# sglang[all]>=0.4.7  -- giant dependency, install manually if needed
openai>=1.0
requests>=2.31
python-dotenv>=1.0
nvidia-ml-py>=13.580

# author packages (PyPI)
packg
typedparser
visiontext

# dev
black
pytest
pytest-cov
